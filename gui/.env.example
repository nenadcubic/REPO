# Optional overrides for gui/docker-compose.yml (Docker Compose reads this file automatically)

# Preset selection: default | dev | demo
GUI_PRESET=default

# Host ports
GUI_HTTP_PORT=8080
GUI_API_PORT=8000

# Optional backend overrides (preset provides defaults)
# ER_REDIS_HOST=redis
# ER_REDIS_PORT=6379
# ER_PREFIX=er
# ER_GUI_LOG_LEVEL=info
# ER_GUI_STORE_PREVIEW_LIMIT=25
# ER_GUI_TTL_MAX_SEC=86400
