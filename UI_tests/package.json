{
  "name": "element-redis-ui-tests",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "install:browsers": "playwright install --with-deps",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}
