{
  "id": "northwind_compare",
  "title": "Northwind: Redis vs SQLite comparison",
  "type": "dataset_compare",
  "description": "Import Northwind tables from a local SQLite DB into Redis (OR layout) and compare row counts and order totals.",
  "default_namespace": "or",
  "tags": ["dataset_compare", "northwind", "sqlite", "or"],
  "reference": { "kind": "sqlite", "path": "assets/northwind.sqlite" },
  "compare_reports": [
    { "id": "row_counts", "title": "Row counts per table" },
    { "id": "order_totals_sample", "title": "Order totals (sample)" }
  ]
}
